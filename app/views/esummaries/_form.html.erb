<%= form_for(esummary) do |f| %>
  <% if esummary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(esummary.errors.count, "error") %> prohibited this esummary from being saved:</h2>

      <ul>
      <% esummary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

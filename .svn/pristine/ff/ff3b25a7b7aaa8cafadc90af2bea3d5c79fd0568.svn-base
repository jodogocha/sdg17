<%= form_for(autority) do |f| %>
  <% if autority.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(autority.errors.count, "error") %> prohibited this autority from being saved:</h2>

      <ul>
      <% autority.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
